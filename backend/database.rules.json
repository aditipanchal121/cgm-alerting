{
  "rules": {
    "devices": {
      "$deviceId": {
        "alert": {
          ".read": "auth != null",
          ".write": "auth != null",
          ".validate": "newData.hasChildren(['severity', 'alertId', 'timestamp'])"
        }
      }
    }
  }
}
