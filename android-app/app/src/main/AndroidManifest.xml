<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".VigilApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Vigil"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Vigil.Launch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen critical alert: launched from VigilFcmService when a
             CRITICAL severity push arrives, so it can wake/interrupt like an alarm
             clock instead of sitting quietly in the notification shade. -->
        <activity
            android:name=".notifications.AlarmActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Vigil.Alarm" />

        <service
            android:name=".notifications.VigilFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Reads IOB directly from a pump app's own notification (e.g. Omnipod 5's
             "Automated Mode" notification) as a more reliable alternative to
             Gluroo's IOB feed - see OmnipodIobListenerService. Inert unless the
             user both enables it in IobSourceSetupScreen and separately grants
             Notification access in system Settings (BIND_NOTIFICATION_LISTENER_SERVICE
             can only be requested that way, never via a runtime permission dialog). -->
        <service
            android:name=".notifications.OmnipodIobListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Home-screen widget showing the latest reading/arrow/IOB - exported
             true because the launcher/AppWidgetManager (a different app/process)
             is what delivers APPWIDGET_UPDATE broadcasts to this receiver. -->
        <receiver
            android:name=".notifications.GlucoseWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/glucose_widget_info" />
        </receiver>

    </application>

</manifest>
