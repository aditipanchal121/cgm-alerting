<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.NightWatch" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Alarm activity theme: shows over the lock screen, so keep it a plain
         full-bleed theme with no action bar chrome. -->
    <style name="Theme.NightWatch.Alarm" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>
